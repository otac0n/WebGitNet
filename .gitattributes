# Auto detect text files and perform line-ending normalization
* text=auto

# Add special handling for common file types.
*.cs     text diff=csharp
*.sln    text merge=union eol=crlf
*.csproj text merge=union
